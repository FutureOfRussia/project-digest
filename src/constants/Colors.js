export default {
  // Main colors
  white(opacity = 1) {
    return `rgba(255, 255, 255, ${opacity})`
  },
  black(opacity = 1) {
    return `rgba(0, 0, 0, ${opacity})`
  },

  // Supporting colors
  disabled: '#F5F4F5',
  transparent: 'transparent',
}
